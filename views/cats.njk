{% extends "layout.njk" %}

{% block content %}
<main>
    <h1>{{ title }}</h1>
    <ul>
        {% for cat in cats %}
        <li>{{ cat.name }}, {{ cat.age }}, {{ cat.breed }} Cat</li>
        {% endfor %}
    </ul>
</main>
{% endblock %}
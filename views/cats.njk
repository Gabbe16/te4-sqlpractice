{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
    <h1>{{ title }}</h1>
    <ul>
        {% for cat in cats %}
        <a href="/dbtest/{{cat.id}}"><li>{{ cat.name }}, {{ cat.age }}, {{ cat.breed }} Cat</li></a>
        {% endfor %}
    </ul>
</main>
{% endblock %}